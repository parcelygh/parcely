{
  "name": "postalservice-monorepo",
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "tsc -b",
    "docs:dev": "pnpm --filter website start",
    "docs:build": "pnpm --filter website build",
    "docs:serve": "pnpm --filter website serve",
    "clean": "pnpm -r clean",
    "check-docs-coverage": "tsx scripts/check-docs-coverage.ts",
    "check-doc-snippets": "tsx scripts/check-doc-snippets.ts",
    "check-treeshake": "tsx scripts/treeshake-check.ts",
    "smoke": "tsx scripts/smoke.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "esbuild": "0.25.10",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
